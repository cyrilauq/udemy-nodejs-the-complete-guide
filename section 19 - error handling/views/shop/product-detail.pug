extends ../layouts/main-layout.pug 
include ../includes/add-to-cart

block main
    main.centered 
        h1 #{prod.title}
        hr
        div 
            img(src=prod.imageUrl, alt=prod.title)
        h2 #{prod.price}
        p #{prod.description}
            +add-to-cart(prod) 